diff -ruN -x .git ldapd/Makefile ldapd-fuzz-with-msg-generator-lib/Makefile
--- ldapd/Makefile	2025-11-26 06:47:25.472165000 +0000
+++ ldapd-fuzz-with-msg-generator-lib/Makefile	2025-11-26 06:41:13.642455000 +0000
@@ -1,3 +1,4 @@
 SUBDIR=	libevent libimsg/src src
+CFLAGS+= -O0 -g
 
 .include <bsd.subdir.mk>
diff -ruN -x .git ldapd/libevent/src/Makefile ldapd-fuzz-with-msg-generator-lib/libevent/src/Makefile
--- ldapd/libevent/src/Makefile	2025-11-26 06:47:26.716939000 +0000
+++ ldapd-fuzz-with-msg-generator-lib/libevent/src/Makefile	2025-11-26 06:40:57.400134000 +0000
@@ -1,3 +1,3 @@
-SUBDIR= lib regress/lib/libevent
+SUBDIR= lib 
 
 .include <bsd.subdir.mk>
Binary files ldapd/libevent/src/regress/lib/libevent/event_regress and ldapd-fuzz-with-msg-generator-lib/libevent/src/regress/lib/libevent/event_regress differ
diff -ruN -x .git ldapd/libimsg/Makefile ldapd-fuzz-with-msg-generator-lib/libimsg/Makefile
--- ldapd/libimsg/Makefile	2025-11-26 06:47:26.725385000 +0000
+++ ldapd-fuzz-with-msg-generator-lib/libimsg/Makefile	2025-11-26 06:40:20.874789000 +0000
@@ -1,3 +1,3 @@
-SUBDIR= src test
+SUBDIR= src
 
 .include <bsd.subdir.mk>
diff -ruN -x .git ldapd/libimsg/src/lib/libutil/Makefile ldapd-fuzz-with-msg-generator-lib/libimsg/src/lib/libutil/Makefile
--- ldapd/libimsg/src/lib/libutil/Makefile	2025-11-26 06:47:26.725537000 +0000
+++ ldapd-fuzz-with-msg-generator-lib/libimsg/src/lib/libutil/Makefile	2025-11-26 06:40:22.382720000 +0000
@@ -1,5 +1,7 @@
 #
 MK_PROFILE=	no
+WITHOUT_PROFILE=yes
+SHLIB_MAJOR=0
 LOCALBASE?=	/usr/local
 SHAREDIR?=	/usr/local
 LIB=		imsg
@@ -10,6 +12,5 @@
 		../libc/stdlib/recallocarray.c compat.h
 INCS=		imsg.h
 MAN=		imsg_init.3
-NO_OBJ=		yes
 
 .include <bsd.lib.mk>
diff -ruN -x .git ldapd/libimsg/src/lib/libutil/imsg-buffer.c ldapd-fuzz-with-msg-generator-lib/libimsg/src/lib/libutil/imsg-buffer.c
--- ldapd/libimsg/src/lib/libutil/imsg-buffer.c	2025-11-26 06:47:26.725667000 +0000
+++ ldapd-fuzz-with-msg-generator-lib/libimsg/src/lib/libutil/imsg-buffer.c	2025-11-26 06:40:23.950221000 +0000
@@ -513,7 +513,7 @@
 #ifdef __OpenBSD__
 	freezero(buf->buf, buf->size);
 #else
-	free(buf->buf);
+	// free(buf->buf);
 #endif
 	free(buf);
 }
diff -ruN -x .git ldapd/src/usr.sbin/ldapd/Makefile ldapd-fuzz-with-msg-generator-lib/src/usr.sbin/ldapd/Makefile
--- ldapd/src/usr.sbin/ldapd/Makefile	2025-11-26 06:47:25.473696000 +0000
+++ ldapd-fuzz-with-msg-generator-lib/src/usr.sbin/ldapd/Makefile	2025-11-26 06:40:25.487172000 +0000
@@ -19,7 +19,7 @@
 SRCS+=		auth_subr.c authenticate.c
 
 LDADD=		${EVENTDIR}/src/lib/libevent/libevent.a
-LDADD+= 	${IMSGDIR}/src/lib/libutil/libimsg.a
+LDADD+= 	${IMSGDIR}/src/lib/libutil/libimsg.so
 
 .ifdef SSLDIR
 LDADD+= 	${SSLDIR}/tls/.libs/libtls.a \
@@ -32,16 +32,25 @@
 .endif
 
 LDADD+= 	-lcrypt -lz -lutil
+LDADD+=		${BUFFER_CHECKER_ROOT}/libbuffer_check.so
+LDADD+=		${BUFFER_CHECKER_ROOT}/libmsg_generator.a
+LDFLAGS+=	-Wl,-rpath,${BUFFER_CHECKER_ROOT}
+LDFLAGS+=	-Wl,-rpath,${IMSGDIR}/src/lib/libutil
+
+LDFLAGS+=	-g -O0 -fno-omit-frame-pointer
 DPADD=		${LIBEVENT} ${LIBTLS} ${LIBSSL} ${LIBCRYPTO} ${LIBZ} ${LIBUTIL}
 CFLAGS+=	-I${.CURDIR} -g
 CFLAGS+=	-Wall -Wstrict-prototypes -Wmissing-prototypes
 CFLAGS+=	-Wmissing-declarations
+CFLAGS+=	-fsanitize-recover=memory
+CFLAGS+=	-g -O0
 CFLAGS+=	-Wshadow -Wpointer-arith -Wcast-qual
 CFLAGS+=	-Wsign-compare
 CFLAGS+=	-I${EVENTDIR}/src/lib/libevent \
 		-I${IMSGDIR}/src/lib/libutil \
 		-I${.CURDIR}/../../lib/libutil \
-		-I${.CURDIR}/../../include
+		-I${.CURDIR}/../../include \
+		-I${BUFFER_CHECKER_ROOT}
 
 .ifdef SSLDIR
 CFLAGS+=	-I${SSLDIR}/include
diff -ruN -x .git ldapd/src/usr.sbin/ldapd/in/fuzz.bin ldapd-fuzz-with-msg-generator-lib/src/usr.sbin/ldapd/in/fuzz.bin
--- ldapd/src/usr.sbin/ldapd/in/fuzz.bin	1970-01-01 00:00:00.000000000 +0000
+++ ldapd-fuzz-with-msg-generator-lib/src/usr.sbin/ldapd/in/fuzz.bin	2025-11-26 06:40:39.421029000 +0000
@@ -0,0 +1 @@
+a
diff -ruN -x .git ldapd/src/usr.sbin/ldapd/ldapd.c ldapd-fuzz-with-msg-generator-lib/src/usr.sbin/ldapd/ldapd.c
--- ldapd/src/usr.sbin/ldapd/ldapd.c	2025-11-26 06:47:25.474683000 +0000
+++ ldapd-fuzz-with-msg-generator-lib/src/usr.sbin/ldapd/ldapd.c	2025-11-26 06:40:27.899941000 +0000
@@ -41,6 +41,7 @@
 
 #include "ldapd.h"
 #include "log.h"
+#include <msg_generator.h>
 
 void		 usage(void);
 void		 ldapd_sig_handler(int fd, short why, void *data);
@@ -57,6 +58,9 @@
 pid_t			 ldape_pid;
 char			*datadir = DATADIR;
 
+void ptr_check_skip(const void *data, size_t size);
+void check_pointers(const void *data, size_t size);
+
 void
 usage(void)
 {
@@ -213,8 +217,10 @@
 	    PF_UNSPEC, pipe_parent2ldap) != 0)
 		fatal("socketpair");
 	
+	/*
 	ldape_pid = start_child(PROC_LDAP_SERVER, saved_argv0,
 	    pipe_parent2ldap[1], debug, verbose, csockpath, conffile);
+	*/
 
 	ldap_loginit("auth", debug, verbose);
 	setproctitle("auth");
@@ -256,6 +262,67 @@
 		err(1, "pledge");
 #endif
 
+	struct imsgbuf other_side;
+	imsg_init(&other_side, pipe_parent2ldap[1]);
+
+	/* Initialize the message generator interface */
+	struct msg_interface iface;
+	/* 1 compartment, 11 message types (1-11) */
+	if (msg_interface_init(&iface, STDIN_FILENO, 1, 11) != MSG_GEN_SUCCESS) {
+		errx(1, "Failed to initialize message interface");
+	}
+
+	/* Message types are 1-11, so we need to offset the default 0-10 mapping */
+	/* The library defaults to 0->0, 1->1, etc., but we want 0->1, 1->2, ..., 10->11 */
+	msg_interface_set_message_types_batch(&iface, 0, 11, 1);
+
+	/* Register the endpoint (compartment 0 = other_side) */
+	msg_interface_set_endpoint(&iface, 0, &other_side);
+
+	/* Set EOM message type */
+	msg_interface_set_eom_type(&iface, IMSG_EOM);
+
+	/* Generate and send messages */
+	struct msg_data msg;
+	int ret;
+
+	while ((ret = msg_generate(&iface, &msg)) == MSG_GEN_SUCCESS) {
+		struct imsgbuf *target_ibuf = (struct imsgbuf *)msg.endpoint;
+
+		if (target_ibuf != NULL) {
+			/* Extract auxiliary data (e.g., peerid and pid) */
+			uint32_t *aux_peerid = (uint32_t *)&msg.aux_data[0];
+			uint32_t *aux_pid = (uint32_t *)&msg.aux_data[4];
+
+			printf("Fuzzing: sending msg_type=%d, size=%u, compartment=%u\n",
+			       msg.type, msg.actual_payload_size, msg.compartment);
+			printf("  aux data: peerid=%u, pid=%u\n", *aux_peerid, *aux_pid);
+
+			ptr_check_skip(msg.payload, msg.actual_payload_size);
+			check_pointers(msg.payload, msg.actual_payload_size);
+
+			/* Send message with or without FD */
+			imsg_compose(target_ibuf, msg.type, *aux_peerid, *aux_pid,
+			             msg.has_fd ? msg.fd : -1,
+			             msg.payload, msg.actual_payload_size);
+
+		}
+	}
+
+	/* Send EOM messages to all compartments */
+	for (uint8_t i = 0; i < iface.num_compartments; i++) {
+		struct msg_data eom_msg;
+		if (msg_generate_eom(&iface, i, &eom_msg) == MSG_GEN_SUCCESS) {
+			struct imsgbuf *target = (struct imsgbuf *)eom_msg.endpoint;
+			if (target != NULL) {
+				imsg_compose(target, eom_msg.type, 0, 0, -1, NULL, 0);
+				imsg_flush(target);
+			}
+		}
+	}
+
+	close(pipe_parent2ldap[1]);
+
 	event_dispatch();
 
 	log_debug("ldapd: exiting");
@@ -272,6 +339,7 @@
 		    __func__, imsg->hdr.type, iev->ibuf.fd);
 		switch (imsg->hdr.type) {
 		case IMSG_LDAPD_AUTH:
+			log_debug("IMSG_LDAPD_AUTH being invoked!!!!");
 			ldapd_auth_request(iev, imsg);
 			break;
 		case IMSG_CTL_LOG_VERBOSE:
@@ -279,6 +347,9 @@
 			break;
 		case IMSG_LDAPD_OPEN:
 			ldapd_open_request(iev, imsg);
+			break;
+		case IMSG_EOM:
+			eom_counter_inc();
 			break;
 		default:
 			log_debug("%s: unexpected imsg %d",
diff -ruN -x .git ldapd/src/usr.sbin/ldapd/ldapd.h ldapd-fuzz-with-msg-generator-lib/src/usr.sbin/ldapd/ldapd.h
--- ldapd/src/usr.sbin/ldapd/ldapd.h	2025-11-26 06:47:25.474819000 +0000
+++ ldapd-fuzz-with-msg-generator-lib/src/usr.sbin/ldapd/ldapd.h	2025-11-26 06:40:38.691336000 +0000
@@ -308,6 +308,7 @@
 	IMSG_LDAPD_AUTH_RESULT,
 	IMSG_LDAPD_OPEN,
 	IMSG_LDAPD_OPEN_RESULT,
+	IMSG_EOM,
 };
 
 struct ns_stat {
Binary files ldapd/src/usr.sbin/ldapd/libbuffer_check.so and ldapd-fuzz-with-msg-generator-lib/src/usr.sbin/ldapd/libbuffer_check.so differ
diff -ruN -x .git ldapd/src/usr.sbin/ldapd/parse.y ldapd-fuzz-with-msg-generator-lib/src/usr.sbin/ldapd/parse.y
--- ldapd/src/usr.sbin/ldapd/parse.y	2025-11-26 06:47:25.475254000 +0000
+++ ldapd-fuzz-with-msg-generator-lib/src/usr.sbin/ldapd/parse.y	2025-11-26 06:40:48.077702000 +0000
@@ -495,6 +495,7 @@
 
 static int	expanding;
 
+__attribute__((no_sanitize("memory")))
 int
 igetc(void)
 {
diff -ruN -x .git ldapd/src/usr.sbin/ldapd/run_fuzzer.py ldapd-fuzz-with-msg-generator-lib/src/usr.sbin/ldapd/run_fuzzer.py
--- ldapd/src/usr.sbin/ldapd/run_fuzzer.py	1970-01-01 00:00:00.000000000 +0000
+++ ldapd-fuzz-with-msg-generator-lib/src/usr.sbin/ldapd/run_fuzzer.py	2025-11-26 06:40:39.791475000 +0000
@@ -0,0 +1,101 @@
+#!/usr/bin/env python3
+import os
+import sys
+import subprocess
+import re
+
+def main():
+    if len(sys.argv) < 2:
+        print("Usage: python run_fuzzer.py <executable> [args...] <folder> [file_id]")
+        print("  executable: path to the program to run")
+        print("  args: optional arguments for the executable")
+        print("  folder: directory containing input files")
+        print("  file_id: optional - specific file ID to run (e.g., 1 for id:000001)")
+        sys.exit(1)
+
+    args = sys.argv[1:]
+
+    # Find the folder (last argument that is a directory, or second-to-last if last is a number)
+    folder = None
+    target_id = None
+    cmd_args = []
+
+    # Check if last argument is a number (file_id)
+    try:
+        target_id = int(args[-1])
+        # If it's a number, the folder should be second-to-last
+        if len(args) < 2:
+            print("Error: Need at least executable and folder arguments")
+            sys.exit(1)
+        folder = args[-2]
+        cmd_args = args[:-2]
+    except (ValueError, IndexError):
+        # Last argument is not a number, so it should be the folder
+        folder = args[-1]
+        cmd_args = args[:-1]
+
+    executable = cmd_args[0] if cmd_args else None
+    exe_args = cmd_args[1:] if len(cmd_args) > 1 else []
+
+    if not executable:
+        print("Error: No executable specified")
+        sys.exit(1)
+
+    if not os.path.isfile(executable):
+        print(f"Error: Executable '{executable}' not found")
+        sys.exit(1)
+
+    if not os.path.isdir(folder):
+        print(f"Error: Folder '{folder}' not found")
+        sys.exit(1)
+
+    # Pattern to match files starting with id:0000XX
+    pattern = re.compile(r'^id:(\d{6})')
+
+    files_to_process = []
+    for filename in os.listdir(folder):
+        match = pattern.match(filename)
+        if match:
+            file_id = int(match.group(1))
+            if target_id is None or file_id == target_id:
+                files_to_process.append((filename, file_id))
+
+    if not files_to_process:
+        if target_id is not None:
+            print(f"No file found with ID {target_id:06d}")
+        else:
+            print("No files found matching pattern id:0000XX")
+        sys.exit(1)
+
+    # Sort by file ID
+    files_to_process.sort(key=lambda x: x[1])
+
+    for filename, file_id in files_to_process:
+        filepath = os.path.join(folder, filename)
+        print(f"Running {executable} with {filename} (ID: {file_id:06d})")
+
+        try:
+            with open(filepath, 'rb') as input_file:
+                cmd = [executable] + exe_args
+                result = subprocess.run(
+                    cmd,
+                    stdin=input_file,
+                    capture_output=True,
+                    timeout=3
+                )
+
+                print(f"  Exit code: {result.returncode}")
+                if result.stdout:
+                    print(f"  Stdout: {result.stdout.decode('utf-8', errors='ignore').strip()}")
+                if result.stderr:
+                    print(f"  Stderr: {result.stderr.decode('utf-8', errors='ignore').strip()}")
+
+        except subprocess.TimeoutExpired:
+            print(f"  Timeout after 3 seconds - killed and continuing")
+        except Exception as e:
+            print(f"  Error: {e}")
+
+        print("-" * 50)
+
+if __name__ == "__main__":
+    main()
\ No newline at end of file
diff -ruN -x .git ldapd/src/usr.sbin/ldapd/schema.c ldapd-fuzz-with-msg-generator-lib/src/usr.sbin/ldapd/schema.c
--- ldapd/src/usr.sbin/ldapd/schema.c	2025-11-26 06:47:25.475368000 +0000
+++ ldapd-fuzz-with-msg-generator-lib/src/usr.sbin/ldapd/schema.c	2025-11-26 06:40:37.195137000 +0000
@@ -277,6 +277,7 @@
 	return nl;
 }
 
+__attribute__((no_sanitize("memory")))
 static int
 schema_getc(struct schema *schema, int quotec)
 {
