diff -ruN a/libimsg/src/lib/libutil/Makefile b/libimsg/src/lib/libutil/Makefile
--- a/libimsg/src/lib/libutil/Makefile	2026-05-01 15:07:57.813188000 -0700
+++ b/libimsg/src/lib/libutil/Makefile	2025-10-29 23:01:40.664046000 -0700
@@ -3,6 +3,7 @@
 LOCALBASE?=	/usr/local
 SHAREDIR?=	/usr/local
 LIB=		imsg
+SHLIB_MAJOR=	0
 LIBDIR=		${LOCALBASE}/lib
 INCSDIR=	${LOCALBASE}/include
 CFLAGS+=	-Werror -Wstrict-prototypes -I${.CURDIR}
diff -ruN a/src/usr.sbin/httpd/Makefile b/src/usr.sbin/httpd/Makefile
--- a/src/usr.sbin/httpd/Makefile	2026-05-01 15:07:56.229319000 -0700
+++ b/src/usr.sbin/httpd/Makefile	2026-05-01 15:10:42.578067000 -0700
@@ -16,17 +16,21 @@
 
 MAN=		httpd.8 httpd.conf.5
 
-LDADD=		../../../libevent/src/lib/libevent/libevent.a \
-		../../../libimsg/src/lib/libutil/libimsg.a
-LDADD+= 	/usr/local/lib/libtls.a \
-		/usr/local/lib/libssl.a \
-		/usr/local/lib/libcrypto.a
-LDADD+= 	-lcrypt -lprocstat -pthread
+LDFLAGS+=	-L${.CURDIR}/../../../libimsg/src/lib/libutil -Wl,-rpath,${.CURDIR}/../../../libimsg/src/lib/libutil
+LDFLAGS+=	-L${LOCALBASE}/lib -Wl,-rpath,${LOCALBASE}/lib
+
+LDADD=		${BUFFER_CHECKER_ROOT}/libmsg_generator.a
+LDADD+=		../../../libevent/src/lib/libevent/libevent.a \
+		../../../libimsg/src/lib/libutil/libimsg.so
+LDADD+= 	-ltls \
+		-lssl \
+		-lcrypto
+LDADD+= 	-lcrypt -lprocstat -pthread -lmd
+LDADD+= 	-L${BUFFER_CHECKER_ROOT} -lbuffer_check
 SRCS+=		patterns.c
 MAN+=		patterns.7
 
 DPADD=		${LIBTLS} ${LIBSSL} ${LIBCRYPTO}
-#DEBUG=		-g -DDEBUG=3 -O0
 CFLAGS+=	-Wall -I${.CURDIR}
 CFLAGS+=	-Wstrict-prototypes -Wmissing-prototypes
 CFLAGS+=	-Wmissing-declarations
@@ -35,7 +38,9 @@
 CFLAGS+=	-I${.CURDIR}/../../../libevent/src/lib/libevent \
 		-I${.CURDIR}/../../../libimsg/src/lib/libutil \
 		-I${.CURDIR}/../../include \
-		-I${LOCALBASE}/include
+		-I${LOCALBASE}/include \
+		-I${BUFFER_CHECKER_ROOT}
+CFLAGS+=	-fsanitize-recover=memory
 CFLAGS+=	-D__dead='' \
 		-DHAVE_CONFIG_H
 CLEANFILES+=	y.tab.h
diff -ruN a/src/usr.sbin/httpd/config.c b/src/usr.sbin/httpd/config.c
--- a/src/usr.sbin/httpd/config.c	2026-05-01 15:07:56.229433000 -0700
+++ b/src/usr.sbin/httpd/config.c	2026-05-01 15:10:50.533371000 -0700
@@ -158,6 +158,42 @@
 	return (0);
 }
 
+
+
+static void
+clear_config_server_ptrs(struct server_config *cfg)
+{
+    if (cfg == NULL)
+        return;
+
+    cfg->default_type.media_encoding = NULL;
+
+    /* clear all fields expanded from RB_ENTRY */
+    memset(&cfg->default_type.media_entry, 0, sizeof(cfg->default_type.media_entry));
+
+    cfg->tls_ca = NULL;
+    cfg->tls_ca_file = NULL;
+    cfg->tls_cert = NULL;
+    cfg->tls_cert_file = NULL;
+    cfg->tls_crl = NULL;
+    cfg->tls_crl_file = NULL;
+    cfg->tls_key = NULL;
+    cfg->tls_key_file = NULL;
+    cfg->tls_ocsp_staple = NULL;
+    cfg->tls_ocsp_staple_file = NULL;
+
+    cfg->logaccess = NULL;
+    cfg->logerror = NULL;
+    cfg->auth = NULL;
+    cfg->return_uri = NULL;
+
+    /* clear TAILQ_HEAD */
+    memset(&cfg->fcgiparams, 0, sizeof(cfg->fcgiparams));
+
+    /* clear TAILQ_ENTRY */
+    memset(&cfg->entry, 0, sizeof(cfg->entry));
+}
+
 int
 config_setserver(struct httpd *env, struct server *srv)
 {
@@ -186,7 +222,9 @@
 		    ps->ps_title[id], srv->srv_s);
 
 		memcpy(&s, &srv->srv_conf, sizeof(s));
+		clear_config_server_ptrs(&s);
 
+
 		c = 0;
 		iov[c].iov_base = &s;
 		iov[c++].iov_len = sizeof(s);
@@ -205,8 +243,10 @@
 					fd = -1;
 				else if ((fd = dup(srv->srv_s)) == -1)
 					return (-1);
+
+
 				if (proc_composev_imsg(ps, id, n,
-				    IMSG_CFG_SERVER, -1, fd, iov, c) != 0) {
+				    IMSG_CFG_SERVER, -1, -1, iov, c) != 0) {
 					log_warn("%s: failed to compose "
 					    "IMSG_CFG_SERVER imsg for `%s'",
 					    __func__, srv->srv_conf.name);
@@ -828,6 +868,7 @@
 		DPRINTF("%s: sending media \"%s\" to %s", __func__,
 		    media->media_name, ps->ps_title[id]);
 
+		memset(&(media->media_entry), 0, sizeof(media->media_entry));
 		proc_compose(ps, id, IMSG_CFG_MEDIA, media, sizeof(*media));
 	}
 
@@ -865,6 +906,7 @@
 	struct privsep		*ps = env->sc_ps;
 	int			 id;
 	unsigned int		 what;
+	struct auth myauth;
 
 	for (id = 0; id < PROC_MAX; id++) {
 		what = ps->ps_what[id];
@@ -875,7 +917,9 @@
 		DPRINTF("%s: sending auth \"%s[%u]\" to %s", __func__,
 		    auth->auth_htpasswd, auth->auth_id, ps->ps_title[id]);
 
-		proc_compose(ps, id, IMSG_CFG_AUTH, auth, sizeof(*auth));
+		memcpy(&myauth, auth, sizeof(struct auth));
+		memset(&myauth.auth_entry, 0, sizeof(myauth.auth_entry));
+		proc_compose(ps, id, IMSG_CFG_AUTH, &myauth, sizeof(*auth));
 	}
 
 	return (0);
diff -ruN a/src/usr.sbin/httpd/httpd.c b/src/usr.sbin/httpd/httpd.c
--- a/src/usr.sbin/httpd/httpd.c	2026-05-01 15:07:56.229712000 -0700
+++ b/src/usr.sbin/httpd/httpd.c	2026-05-01 15:13:14.131672000 -0700
@@ -44,6 +44,7 @@
 
 #include "httpd.h"
 #include "getdtablecount.h"
+#include "msg_generator.h"
 
 #define MAXIMUM(a, b)	(((a) > (b)) ? (a) : (b))
 
@@ -113,6 +114,7 @@
 	exit(1);
 }
 
+
 int
 main(int argc, char *argv[])
 {
@@ -266,6 +268,58 @@
 	if (parent_configure(env) == -1)
 		fatalx("configuration failed");
 
+
+	///////
+
+
+	struct imsgbuf server_ibuf;
+	int server_fd = ps->ps_pipes[PROC_SERVER][0].pp_pipes[PROC_PARENT][0];
+	imsg_init(&server_ibuf, server_fd);
+
+	struct imsgbuf logger_ibuf;
+	int logger_fd = ps->ps_pipes[PROC_LOGGER][0].pp_pipes[PROC_PARENT][0];
+	imsg_init(&logger_ibuf, logger_fd);
+
+	/* Initialize the message generator interface:
+	 * 2 compartments (0=server, 1=logger), 22 message types (IMSG_NONE..IMSG_EOM) */
+	struct msg_interface iface;
+	if (msg_interface_init(&iface, STDIN_FILENO, 2, 22) != MSG_GEN_SUCCESS)
+		errx(1, "Failed to initialize message interface");
+
+	msg_interface_set_endpoint(&iface, 0, &server_ibuf);
+	msg_interface_set_endpoint(&iface, 1, &logger_ibuf);
+	msg_interface_set_eom_type(&iface, IMSG_EOM);
+
+	/* Generate and send messages until fuzzer input is exhausted */
+	struct msg_data msg;
+	int ret;
+
+	while ((ret = msg_generate(&iface, &msg)) == MSG_GEN_SUCCESS) {
+		struct imsgbuf *target_ibuf = (struct imsgbuf *)msg.endpoint;
+
+		if (target_ibuf != NULL) {
+			imsg_compose(target_ibuf, msg.type, 0, 0,
+			    msg.has_fd ? msg.fd : -1,
+			    msg.payload, msg.actual_payload_size);
+			imsg_flush(target_ibuf);
+		}
+	}
+
+	/* Send EOM to all compartments */
+	for (uint8_t i = 0; i < iface.num_compartments; i++) {
+		struct msg_data eom_msg;
+		if (msg_generate_eom(&iface, i, &eom_msg) == MSG_GEN_SUCCESS) {
+			struct imsgbuf *target = (struct imsgbuf *)eom_msg.endpoint;
+			if (target != NULL) {
+				imsg_compose(target, eom_msg.type, 0, 0, -1, NULL, 0);
+				imsg_flush(target);
+			}
+		}
+	}
+
+
+	///////
+
 	event_dispatch();
 
 	parent_shutdown(env);
@@ -284,6 +338,7 @@
 	struct media_type	*media;
 	struct auth		*auth;
 
+	memset(&cf, 0, sizeof(cf));
 	RB_FOREACH(media, mediatypes, env->sc_mediatypes) {
 		if (config_setmedia(env, media) == -1)
 			fatal("send media");
@@ -420,6 +475,10 @@
 	case IMSG_CFG_DONE:
 		parent_configure_done(env);
 		break;
+	case IMSG_EOM:
+		eom_counter_inc();
+		break;
+
 	default:
 		return (-1);
 	}
@@ -459,6 +518,9 @@
 	case IMSG_LOG_OPEN:
 		if (logger_open_priv(imsg) == -1)
 			fatalx("failed to open log file");
+		break;
+	case IMSG_EOM:
+		eom_counter_inc();
 		break;
 	default:
 		return (-1);
diff -ruN a/src/usr.sbin/httpd/httpd.h b/src/usr.sbin/httpd/httpd.h
--- a/src/usr.sbin/httpd/httpd.h	2026-05-01 15:07:56.229987000 -0700
+++ b/src/usr.sbin/httpd/httpd.h	2025-11-01 15:10:25.727981000 -0700
@@ -222,7 +222,8 @@
 	IMSG_LOG_ACCESS,
 	IMSG_LOG_ERROR,
 	IMSG_LOG_OPEN,
-	IMSG_TLSTICKET_REKEY
+	IMSG_TLSTICKET_REKEY,
+	IMSG_EOM
 };
 
 enum privsep_procid {
diff -ruN a/src/usr.sbin/httpd/logger.c b/src/usr.sbin/httpd/logger.c
--- a/src/usr.sbin/httpd/logger.c	2026-05-01 15:07:56.230122000 -0700
+++ b/src/usr.sbin/httpd/logger.c	2025-10-22 21:57:49.292603000 -0700
@@ -166,11 +166,16 @@
 	size_t			 len;
 	int			 fd;
 
+	// memset(name, 0, sizeof(name));
+	memset(path, 0, sizeof(path));
 	/* called from the privileged process */
 	IMSG_SIZE_CHECK(imsg, &id);
+	if (IMSG_DATA_SIZE(imsg) == sizeof(id))
+		exit(-1);
 	memcpy(&id, imsg->data, sizeof(id));
 	p = (char *)imsg->data + sizeof(id);
 
+	p[IMSG_DATA_SIZE(imsg) - sizeof(id) - 1] = '\0';
 	if ((size_t)snprintf(name, sizeof(name), "/%s", p) >= sizeof(name))
 		return (-1);
 	if ((len = strlcpy(path, httpd_env->sc_logdir, sizeof(path)))
diff -ruN a/src/usr.sbin/httpd/parse.y b/src/usr.sbin/httpd/parse.y
--- a/src/usr.sbin/httpd/parse.y	2026-05-01 15:07:56.230401000 -0700
+++ b/src/usr.sbin/httpd/parse.y	2025-11-08 21:38:51.042486000 -0800
@@ -1516,6 +1516,7 @@
 
 static int	expanding;
 
+__attribute__((no_sanitize("memory")))
 int
 igetc(void)
 {
@@ -1900,6 +1901,7 @@
 	struct media_type	 m;
 	int			 i;
 
+	// memset(&m, 0, sizeof(m));
 	conf = x_conf;
 	conf->sc_flags = 0;
 
diff -ruN a/src/usr.sbin/httpd/proc.c b/src/usr.sbin/httpd/proc.c
--- a/src/usr.sbin/httpd/proc.c	2026-05-01 15:07:56.230551000 -0700
+++ b/src/usr.sbin/httpd/proc.c	2026-05-01 15:12:22.036955000 -0700
@@ -111,8 +111,9 @@
 			snprintf(num, sizeof(num), "%u", i);
 
 			fd = ps->ps_pipes[p->p_id][i].pp_pipes[PROC_PARENT][0];
-			ps->ps_pipes[p->p_id][i].pp_pipes[PROC_PARENT][0] = -1;
+			// ps->ps_pipes[p->p_id][i].pp_pipes[PROC_PARENT][0] = -1;
 
+#if 0
 			switch (fork()) {
 			case -1:
 				fatal("%s: fork", __func__);
@@ -148,6 +149,7 @@
 				close(fd);
 				break;
 			}
+#endif
 		}
 	}
 	free(nargv);
@@ -225,6 +227,7 @@
 					fatal("%s: socketpair", __func__);
 
 				pa->pp_pipes[dst][proc] = fds[0];
+				
 				pb->pp_pipes[PROC_PARENT][0] = fds[1];
 			}
 		}
@@ -257,6 +260,9 @@
 	struct privsep_pipes	*pp = ps->ps_pp;
 	struct imsgev		*iev;
 
+	if (dst != 0 || n != 0)
+		exit(-1);
+
 	if (ps->ps_ievs[dst] == NULL) {
 #if DEBUG > 1
 		log_debug("%s: %s src %d %d to dst %d %d not connected",
@@ -268,6 +274,7 @@
 		return;
 	}
 
+
 	if (pp->pp_pipes[dst][n] != -1) {
 		log_warnx("%s: duplicated descriptor", __func__);
 		close(fd);
@@ -423,6 +430,7 @@
 
 			pf.pf_procid = src;
 			pf.pf_instance = i;
+
 			if (proc_compose_imsg(ps, dst, j, IMSG_CTL_PROCFD,
 			    -1, pb->pp_pipes[src][i], &pf, sizeof(pf)) == -1)
 				fatal("%s: proc_compose_imsg", __func__);
@@ -642,7 +650,7 @@
 		if (n == 0)
 			break;
 
-#if DEBUG > 1
+#if 0
 		log_debug("%s: %s %d got imsg %d peerid %d from %s %d",
 		    __func__, title, ps->ps_instance + 1,
 		    imsg.hdr.type, imsg.hdr.peerid, p->p_title, imsg.hdr.pid);
@@ -780,10 +788,11 @@
 	int	 m;
 
 	proc_range(ps, id, &n, &m);
-	for (; n < m; n++)
+	for (; n < m; n++) {
 		if (imsg_composev_event(&ps->ps_ievs[id][n],
 		    type, peerid, ps->ps_instance + 1, fd, iov, iovcnt) == -1)
 			return (-1);
+	}
 
 	return (0);
 }
@@ -825,20 +834,14 @@
 int
 proc_flush_imsg(struct privsep *ps, enum privsep_procid id, int n)
 {
-	struct imsgbuf	*ibuf;
-	int		 m, ret = 0;
-
-	proc_range(ps, id, &n, &m);
-	for (; n < m; n++) {
-		if ((ibuf = proc_ibuf(ps, id, n)) == NULL)
-			return (-1);
-		do {
-			ret = imsg_flush(ibuf);
-		} while (ret == -1 && errno == EAGAIN);
-		if (ret == -1)
-			break;
-		imsg_event_add(&ps->ps_ievs[id][n]);
-	}
-
-	return (ret);
+	/*
+	 * Harness: skip the imsg_flush retry loop entirely.  In the
+	 * single-process fuzzing build the parent and "child" compartments
+	 * share the same socketpair, so a synchronous flush here would
+	 * just deadlock against itself.
+	 */
+	(void)ps;
+	(void)id;
+	(void)n;
+	return (0);
 }
